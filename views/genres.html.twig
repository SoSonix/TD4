{% extends 'layout.html.twig' %}

{% block content %}
<h2>Genres</h2>

<ul>
{% for genre in genres %}
    <li><a href="{{ path('filmGenre',{id: genre.id}) }}">{{ 'genres.nom' }}" ({{ genres.nbFilms}})</li>
{% endfor %}
</ul>
{% endblock %}
