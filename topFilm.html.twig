{% extends 'layout.html.twig' %}

{% block content %}
<h2> Films les mieux notés </h2>
{% if topFilm %}
    {% for film in topFilm %}
    <div class="film">
	        <img width="64" src="{{ film.image }}" />
	        <h3>{{ film.nom }}</h3>
			<a><u>Moyenne</u>: {{film.moy}}/5</a>
			<br/>
            <a href="{{ path('film', {id: film.id}) }}">Fiche film ;</a>
	</div>	
    {% endfor %}
{% else %}
    Il n'y a aucun film!
{% endif %}

{% endblock %}